{
  "name": "@ods/website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "NODE_ENV=production astro build --mode production",
    "preview": "astro preview",
    "typecheck": "astro check",
    "deploy": "NODE_ENV=production npm run build && firebase deploy --only hosting --project prod-ods-260726"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.5",
    "astro": "^7.2.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.7",
    "typescript": "^5.7.3"
  }
}
